---
import { getCollection } from 'astro:content';
import SectionPage from '../../components/SectionPage.astro';
import { sortEntries } from '../../lib/content';
const entries = sortEntries(await getCollection('entries', ({ data }) => data.section === 'writing' && !data.draft));
---
<SectionPage section="writing" entries={entries} />
